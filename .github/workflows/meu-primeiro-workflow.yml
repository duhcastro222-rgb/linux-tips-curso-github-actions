name: Primeiro workflow

on:
    push:
        branches:
            - main


jobs:
    giropops:
        runs-on: ubuntu-latest
        steps:
            - name: Confere versao Ubuntu
              run : cat /etc/os-release

            -name : Exibir uma mensagem
             run : echo "Estou fazendo o curso do LinuxTips"
             